// instantianted new array and added 5 numeric values
let myAim = [10, 20, 30, 40, 50];

// output first value of array
console.log("First Value of array:", numbers[10]);

// output the last value of the array
console.log("Last Value of array:", numbers[50]);

// The numbers that would have to get passed as indexes would be all of
// them as they are split up with the values 0-4, we call these values by using console log since arrays

console.log("Values within the array");
for (let i = 0; i < numbers.length; i++) {
    console.log(numbers[i]);
}

// pushing number
numbers.push(69, 17, 99, 42, 34);

// output contents
console.log("This is the Array after the Push:", numbers);

// using pop
numbers.pop();
numbers.pop();
numbers.pop();

// output contents of array

console.log("Array after 3 pops:", numbers);

//declare string
let myString = "Hello, Jonathan!";

console.log("Length of myString:", myString.length);





